#######
Homeboy
#######

Homeboy is a Salt formula which installs users' dotfiles from a git repository,
runs any specified installation command on them, and installs system and
system-Python packages specified by users in their pillar data.
